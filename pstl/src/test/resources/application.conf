akka.loggers = [
  akka.testkit.TestEventListener
]

pstl.job {
  await-leadership-timeout = 15s
  retry-min-backoff = 1s
  retry-max-backoff = 1h
  retry-random-factor = 0.1

}
pstl.metrics {
  http-host = "0.0.0.0"
  http-port = 8700
}
